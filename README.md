# MetronomeNext

MetronomeNext is a metronome web app that lets you:

* Select the sound
* Set the accent
* Choose the beat count
* Adjust the tempo

Built with **Next.js** and **TypeScript**, the app is:

* **Responsive** – works on any device
* **Optimized** – uses the **Web Audio API** for smooth, accurate timing
